OS = os.

sudo apt update && sudo apt upgrade && sudo apt autoremove

# install software

sudo apt install wget curl lazygit neovim git
sudo curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyuddn
curl https://pyenv.run | bash
